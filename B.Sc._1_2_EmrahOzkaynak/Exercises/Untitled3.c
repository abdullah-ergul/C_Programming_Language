#include <stdio.h>
#include <string.h>

struct birthday{
	int day;
	int month;
	int year;
};

int main(){
	scanf("%d %d %d",&(struct birthday.day), &(struct birth.month), &(struct birth.year));
	printf("%d/%d/%d is your birthday",birth.day, birth.month, birth.year);
	
	return 0;
}
